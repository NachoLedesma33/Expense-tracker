{
    "buildCommand": "export DJANGO_SETTINGS_MODULE=config.settings.production && python manage.py tailwind build && python manage.py collectstatic --noinput && python manage.py migrate --noinput",
    "installCommand": "pip install -r requirements.txt",
    "outputDirectory": "staticfiles",
    "routes": [
        { "src": "/static/(.*)", "dest": "/static/$1" },
        { "src": "/(.*)", "dest": "/api/index.py" }
    ]
}
